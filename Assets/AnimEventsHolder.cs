using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimEventsHolder : MonoBehaviour
{
    //public List<AnimEvents> animEvents = new List<AnimEvents>();

    //public List<AnimEvents> FindEvents(List<string> names)
    //{
    //    List<AnimEvents> events = new List<AnimEvents>();
    //    foreach (var n in names)
    //    {
    //        foreach (var e in animEvents)
    //        {
    //            if (e.eventName == n)
    //            {
    //                events.Add(e);
    //                //Debug.Log("found " + e.eventName);
    //                break;
    //            }
    //            events.Add(null);
    //        }
    //    }
        
    //    return events;
    //}
}
